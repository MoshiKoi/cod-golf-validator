[package]
name = "cod-golf-validator"
version = "0.1.0"
edition = "2024"

[dependencies]
wasm-bindgen = "0.2.126"

[lib]
crate-type = ["cdylib"]

[profile.release]
lto = true
opt-level = 'z'
strip = true
codegen-units = 1
panic = "abort"
